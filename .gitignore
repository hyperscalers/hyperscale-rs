# Rust
.cargo/config.local.toml
/target/
**/*.rs.bk
*.pdb

# IDE
.vscode/
.idea/
*.swp
*.swo
*~
.DS_Store

# Testing
/target/
/simulation-failures/
/reference/

# Proptest failure seeds: one machine's record of one run, written the
# moment a property fails and never read back deliberately — so tracking
# them turns every incidental failure into a diff to review.
*.proptest-regressions
proptest-regressions/

# Benchmarks
/target/criterion/
profile.json.gz

# Temporary files
*.tmp
*.temp

# Logs
*.log

# RocksDB
*.sst
*.log
MANIFEST-*
CURRENT
IDENTITY
LOCK
OPTIONS-*
LOG*

# Validator data
data/
keys/
cluster-data/
.hyperscale-nonces.json
bootstrap-data/
distributed-cluster-data/
docker-cluster-data/

# Modelling
tla/
_apalache-out/
__pycache__/
*.pyc

# Docker compose and monitoring (dynamically generated)
scripts/monitoring/prometheus.yml
scripts/docker-compose.generated.yml

# Terraform
**/.terraform/
*.tfstate
*.tfstate.*
*.tfvars
!*.auto.tfvars
.terraform.lock.hcl
# Assembled site bundle
site/dist/
