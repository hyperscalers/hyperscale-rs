# Protocol Models

Machine-checked models of hyperscale's protocols, written in [Quint](https://quint-lang.org/) and checked with Apalache (`quint verify`). The properties come from the invariant register ([docs/08-invariants.md](../docs/08-invariants.md)); models cite INV-* IDs rather than restating them.

## Model register

| Model | File | Scope | Properties | Status |
|---|---|---|---|---|
| **A** | [wt_clock.qnt](wt_clock.qnt) | The weighted-time clock: per-vote clamp, quorum mean, admission plausibility, epoch resolution | INV-SHARD-6, INV-BEACON-3/4 | Verified |
| **B** | [shard_consensus.qnt](shard_consensus.qnt) | Shard consensus (the HotStuff-2 variant): safe-vote rule, round-contiguous commit, crash recovery. The healthy instance's never-reset registers are the implementation's durable ones; the broken twin loses them to QC-floor re-init: one crash + F Byzantine forks the commit (scripted witness + Apalache search) | INV-SHARD-1..4 | Verified (depth 8) |
| **C** | [atomic_commitment.qnt](atomic_commitment.qnt) | Atomic commitment with shard consensus as a commit oracle: provision → execute → certify under per-shard attested deadlines. Abort dominance + success unanimity carry INV-EXEC-1 through deadline heterogeneity, late provisions, and every delivery interleaving. The broken twin is an equivocating committee against first-wins EC admission — split-brain finalization. Conflict tiebreak (INV-EXEC-4) and multi-tx wave grouping deferred to a revision | INV-EXEC-1/5 (4 deferred) | Verified (depth 10) |
| **D** | [straddler_settlement.qnt](straddler_settlement.qnt) | Straddler settlement layered on C: the settled-set fence over a shard scheduled to terminate, the attested window's coverage, sweeps, late-EC materialization. Healthy instance holds atomicity; twins remove the scheduled-terminal layer (`straddler_preboundary`), the fence entirely (`straddler_unfenced`, H2), or shrink the attested window to a terminal-relative span (`straddler_windowed` — the sweep aborts a wave the terminating shard settled). Cross-validated against the implementation by the `split_straddler_ec_partition_atomic` sim test and the production-epoch straddler sims | INV-RESHAPE-5/6 | Verified |
| **E** | [beacon_spc.qnt](beacon_spc.qnt) | Beacon consensus, three layers. The inner PC value algebra — the Prefix Consensus protocol of [arXiv:2602.02892](https://arxiv.org/abs/2602.02892) (Algorithm 1), checked against the paper's own properties: Upper Bound (directed), low/high consistency (Cor. 2.3, Def. 8.1), and Validity, regardless of input coverage. Twins: the two-round variant — the paper's Algorithm 7 (sound only at n ≥ 5f+1) run at n = 3f+1, where Theorem 4.1 proves two rounds impossible; the counterexample is that boundary, mechanized — and the eager/partial input feed (a 2-2 input split collapses the committed low to empty without violating any paper property; the full-coverage instance pins the non-collapse the shipped feed gate buys). The epoch layer is pool ratification: `beacon_epoch_ratified` demotes the SPC cert to a proposal certificate and commits both outcomes through one two-phase locked pool quorum (single-shot Tendermint over block hashes, standard 2f+1 quorum) — `invAll` holds at pool ≫ committee, at pool == committee, and on a three-round re-lock instance — all against the committee-sized Byzantine wildcard (F = 1), below the larger pools' own tolerance; a pool-scaled adversary is deferred (see the header's modeling assumptions); the phases are load-bearing (a strict one-vote register wedges on a near-deadline split, and the single-phase re-vote twin `beacon_epoch_ratified_direct` re-forks). The broken twin is the two-path family (`beacon_epoch`: SPC cert and pool skip cert as independent commits): the quorums need not intersect, and `poolQuorumFork` dual-commits with every signature honest-conformant once the pool outgrows the committee — sign-path exclusion is safe only at pool == committee. The view layer (`spc_views`, Algorithm 3, per-view PC as oracle) checks SPC Agreement — every completed commit walk resolves the same view-1 high — and view-1 unskippability. Three gates carry it, one twin each: value-function parent resolution (first non-bottom element only; the fall-through twin re-creates the pre-2026-07 `parent_of` deviation and forks two replicas on one committed value with zero Byzantine action), the decided-parentless-high gate on honest empty-view attestations (the attest-on-timeout twin skips a committed view — Upper Bound is what makes "my high is parentless" mean "nothing committed here"), and the f+1/max-reported indirect-cert rule (Lemmas A.3–A.5; the threshold-f twin forks via an all-Byzantine skip cert). A fourth PC-layer twin re-signs rounds 2/3 from the since-grown vote pool — the re-aggregating retransmission the heartbeat's emission-recorded QCs (`sent_vote2/3`) forbid — and forks the decisions, pinning the replay-verbatim rule the SPC retransmission heartbeat rests on. Deferred to a revision: PC liveness, view-timeout timing, and cross-epoch chaining | INV-BEACON-1 (single epoch) | Verified (depths 5–10 per instance) |
| **F** | [reshape_lifecycle.qnt](reshape_lifecycle.qnt) | The beacon-side reshape fold: trigger admission (pool gate, re-assertion refresh), frozen-seed cohort draw, readiness, the 2f+1-per-successor seating gate, TTL lapse/abandon, and shuffle staffing (split-skip, keeper-pin, top-up). Two modules — the shard-side ready-signal pool as an abstract-tick transport (`invDwellMonotone`, pinning the shipped dwell fix) and the pure fold layered on it. The gate is checked at its *semantic* floor: f+1 honest genuinely-synced members per successor, not merely 2f+1 counted seats. Twins: `reshape_dwell_reset` (admission resets dwell → a live observer's signal never drains), `reshape_epoch_seeded_draw` (live-seeded cohort draw → the joiner child-swap), `reshape_short_draw` (keeper draw counted against drawn rather than ready size), and `reshape_unbound_ready` — the load-bearing one: the ready signal binds only `(network, validator, wt_window)` and the fold classifies it by the emitter's *current* seat, so a signal retained across a split lapse re-binds to a different child under pool churn and marks a seat ready that never synced (Apalache counterexample, no Byzantine actor). Two adjacent results sharpen it: pool churn drifts a frozen-seed re-staff off an observer's first assignment, so `invSeatStability` — INV-RESHAPE-7's "cannot move an observer's sync target" — fails for a *churned* pool, not only an epoch-seeded one; but that drift is benign under a child-bound signal (`reshape_split_churn` holds `invNoHonestFalseReady`), which is what makes binding the signal to its child the fix — now shipped: the ready signal binds its attested shard, the `ReshapeReady` leaf carries it, and the fold credits readiness only when `seat.child` matches. The finding is liveness, not safety (the f+1 honest floor is never breached — parent halves hold it). The shuffle's recovering-shard clause (INV-SEC-2) is checked by Model G, which carries the recovery state this model does not | INV-RESHAPE-1/7/8, INV-SEC-2 (reshape clauses) | Verified |
| **G** | [halt_recovery.qnt](halt_recovery.qnt) | The f+1 liveness recovery, three modules, whose adversary is the one no other model carries: the retained committee holds f+1..2f corrupt — the recovery's premise is that the `BYZ ≤ F` axiom failed. `halt_detection` — the beacon fold that flags a halted shard (miss counting over observed folds, the refresh-vs-miss rule, reshape/terminal/placeholder exemptions, the all-or-nothing full re-draw, retention fold-forward, shuffle-skip and eligible sit-out); healthy holds `invAll` exhaustively (detection soundness, no false flag, retention, hygiene), twins `halt_refresh_on_refold` (the stale-crossing miss-reset — a halted shard that never flags) and `halt_wallclock_watermark` (a beacon drought false-flags every shard). `recovery_bridge` — abstract consensus with Model B as the per-committee oracle and Model A as the clock, the QC weighted timestamp modeled as the faithful clamped mean of wall-clock vote stamps. **Finding G-1 (adjudicated): the certified re-bind's one-window tolerance is calibrated for an adversarial *minority*, but the retained committee is beyond f, so its corrupt majority drags the QC mean arbitrarily below wall-clock into the suffix band, escaping the re-bind — a folded replica admits an orphan two-chain commit past the halted tip** (`invNoStaleCommit` Apalache CE; the `honest_old` control with zero corrupt holds `invAll`, isolating the timestamp drag; the fresh side, `invSuffixVerifiable`/`invBridgeBindable`/`invBridgeEmpty`, holds). `cross_shard_freeze` — the escalation and its fix: the halted committee's forged ECs export cross-shard (consumers resolve the source committee by anchor window, no bridge), turning the f+1 halt into an INV-EXEC-1 cascade; the fix is a beacon-mandated freeze that revokes the old committee's cross-shard authority network-wide at the detection fold (`invFenceHolds`/`invResidualBounded` hold with the fence, the `no_freeze` twin violates), with the residual bounded to the pre-fold detection-latency window. The lagging-replica question resolves to: prevention (the freeze) closes the leak forward; already-extended cross-shard trust is unrecoverable, so f+1..2f is a bounded-exposure regime, not a clean recovery (SEC-1 §10.1). The freeze holds in code — `recovery_fences` (EC path) and `recovery_resolves_retained` (boundary + remote-header paths) reject the retained committee's post-frontier artifacts network-wide, and the local orphan is left to INV-STATE-5's loud halt (no sound pre-emptive gate exists against a beyond-f committee). Statistical grind resistance of the re-draw stays with SEC-1 (§10 table S) | INV-SEC-8/9, INV-SEC-2 (recovery clause) | Verified; fix shipped |
| **H** | [witness_fold.qnt](witness_fold.qnt) | The beacon-witness fold's window arithmetic — the surface the epoch randomness rides. `witness_fold` — one shard's accumulator folded by the committed epoch sequence plus one reshape generation: chunk consumption `[watermark, min(watermark + cap, boundary_count))`, fresh-crossing vs drain re-fold bookkeeping, the halt-recovery fence (a pending recovery's post-frontier chunk mutates state but never seeds; the frontier is the recorded crossing's own position, so the halted chain's legitimate backlog seeds on recovery), and the reseat (successors start fresh accumulators while the predecessor's terminal record drains concurrently; split children and a composed merge parent are identical at this abstraction). Exactly-once (`invNoDoubleFold`), fence soundness (`invFence`), and coverage (`invCoverage` — every applied leaf seeded xor deterministically fenced, none skipped or phantom) hold; recovery stamping is any-moment, over-approximating Model G's detector. Twins: `witness_fold_reporter_range` folds a reporter-chosen range instead of the local watermark — a re-sent chunk double-folds leaves the seed already consumed, pinning why `chunk_bounds` derives the range locally — and `witness_fold_no_fence` seeds the completing crossing's post-frontier chunk under a pending recovery, handing the beyond-f retained committee a seed lever. `witness_window` — the base-freeze discipline: a window's active write (promotion at epoch open, before the fold advances any watermark) and its lookahead write (the prior epoch's close) read the same between-folds watermark (`invWindowAgree`); the `witness_window_live_base` twin defers the active write past the fold and the two writes of one window diverge — the beacon-witness-root fork the freeze prevents | INV-BEACON-2 (witness-fold clauses), INV-BEACON-3 (base-freeze bookend) | Verified |

Models compose by abstraction, not size: each takes the earlier models' verified properties as axioms. B resolves committees through A's clock properties; C treats a shard chain as an atomic commit oracle justified by B; D adds shard termination on top of C; F models the staffing lifecycle that produces the successor committees D's terminating shards hand off to, taking A's epoch resolution as an oracle; H folds the witness stream those committees certify, taking G's halt detection as the environment that stamps its fence. No model ever contains the whole system.

## Analyses

Statistical companions to the models — properties that are probabilistic rather than reachability claims, so the instrument is concentration bounds and Markov analysis rather than Apalache.

| Analysis | Files | Scope | Status |
|---|---|---|---|
| **SEC-1** | [committee_security.md](committee_security.md), [committee_security.py](committee_security.py), `crates/beacon/tests/committee_security.rs` | Prices the INV-SEC-1 premise: hypergeometric draw tails, the trickle shuffle as a birth–death chain, the seat-vs-stake wedge, the beacon committee's per-epoch redraw, and the adaptive adversary computed exactly (transient compromise probability + value iteration over corrupt-seats/in-flight/budget; the optimal policy conserves budget and spends on lucky rotations). §10 prices randomness steering against the two failure boundaries: the f+1 *liveness* halt is recoverable (beacon-detected full-committee re-draw plus the cross-shard freeze), while the *terminal* 2f+1 is prevention-only — and unsteered sampling tolerates β ≤ 0.40 at the terminal boundary (table V), so steering is the only route there. The epoch seed removes the primary steering lever structurally: reveal leaves range-folded from the beacon-witness accumulator, no include/omit toggle, interior leaves blind by certified position; the residual is a β-gated window-edge best-of-≈2, below the ceremony baseline at every shard count with a FIFO equilibrium far under f+1 (tables W1–W4). The fallback ceremony (zero-crossing epochs only — self-announcing when adversarially induced) is grindable best-of-`2^t` and bounded by the input-side stack — b=16, FIFO eviction, jail-on-first held for a recency period, a recency-weighted resample — first 2f+1 crossing at β ≈ 0.14, outside the sampling budget's own 0.131 line (tables M/N/R/T). Every model's `BYZ = 1..F` axiom composes with this bound. The Monte Carlo tier drives the production fold and validates the chain's kernel against it (the seeded cells run in CI); measured deviations are priced in §7, and operator concentration is priced in the seat-cost table | Priced + dispositioned; measurement tier in CI |

## Discipline

1. **Properties come from the register.** A spec states which INV-* IDs it checks and under which abstraction. If a property isn't in the register, it goes into the register first.
2. **Transitions come from the code, not the docs.** Every modeled rule names its implementing code (crate/file + function) in a comment. The docs are the map; the code is the territory — a model transcribed from prose verifies the documentation. Deviations between model and implementation (simplified quorum counting, symmetry reductions) are stated explicitly with the argument for why they preserve the checked properties.
3. **Assumptions live in the header.** What is idealized (crypto, network), what is an oracle (components verified by an earlier model), and the encoding choices Apalache forces.
4. **Every model ships a broken twin.** Alongside the healthy instance, an instance whose constants or rules violate the property's precondition, with the counterexample reachable by `quint verify`. A checker that has never produced a counterexample against the model proves nothing about the model.
5. **Three tiers, in order.** `quint typecheck` (syntax/types), `quint run --invariant=...` (fast random search), `quint verify --invariant=...` (Apalache: exhaustive over all behaviors to a bounded depth — default 10 steps, raise with `--max-steps`). Bounded checking is not proof; inductive invariants are the upgrade path once a model stabilizes.
6. **Findings close the loop.** A counterexample — or a suspiciously easy pass — is not a result until it is traced through the implementation from first principles and resolved as a code change, a doc change, or a documented non-issue.

## Apalache notes

- Symbolic mode requires **constant integer ranges**: encode state-relative quantities as offsets in a constant band (never `a.to(b)` with a state- or nondet-dependent bound), and pick subsets via `powerset()` with a size guard rather than an index range.
- **[Choreo](https://github.com/quint-co/choreo) is `quint run`-only.** Its row-polymorphic core fails Apalache's type recovery on Choreo's own examples (2PC: Snowcat error on `choreo::s`; MonadBFT: JVM crash), and its test suite never invokes `verify`. Models here are plain Quint; Choreo's file organization (types/protocol/instance split, explicit local-state records) is still worth imitating.
- `quint verify` downloads Apalache automatically on first use; a JDK must be on the PATH.
- Every `quint verify` targets the server at `localhost:8822` by default, and the shared JVM's lifetime is tied to the process tree that spawned it — a sibling's exit can drop every in-flight check. Give concurrent runs their own `--server-endpoint=localhost:<port>` (one JVM per check); a single check is sequential (one core, modest heap), so per-check isolation is also the only parallelism available. `JVM_ARGS="-Xmx16g"` raises the heap if a deep sweep needs it.
- Counterexamples land in `_apalache-out/` (gitignored), including ITF JSON traces — the format a future model-based-testing bridge can replay against the Rust state machines.
